---
interface Props {
  title?: string;
}
const { title = "🚧 Under Construction" } = Astro.props;
---

<div class="notice">
  <h2>{title}</h2>
  <slot>
    This website and the Runestaff language are actively being developed. I
    don't know why are you here, but it is published right now only to add links
    to my assembler with error references.
  </slot>
</div>

<style>
  .notice {
    background: var(--sl-color-orange-low);
    border: 2px solid var(--sl-color-orange);
    border-radius: 8px;
    padding: 1.5rem;
    margin: 2rem 0;
  }
  .notice h2 {
    margin-top: 0;
    color: var(--sl-color-orange);
  }
</style>
